I've investigated the issue, and the Server error (500): internal server error you're seeing on the Files page is originating directly from the remote backend server at https://newsandvulst.mrpent.us/api/v1/files.

The ThreatIntelApiService.cs in the MAUI app correctly catches this HTTP 500 response from the server and maps it to the error message you are seeing in the UI.

Since my workspace is restricted to the MAUI app codebase (ThreatIntelClient), I don't have write access to edit your backend codebase (NewsandVulSBE_ST) directly, but here is what is happening and how it can be fixed:

Root Cause Analysis
I checked the local copy of the backend code you have running in Docker (newsandvulsbe_st-api-1) which mirrors your remote server. The 500 error is caused by an unhandled exception thrown in FilesController.ListFiles or in the core database configurations.

Additionally, I noticed another 500 Internal Server error occurring locally during Sign-Up because the NormalizedEmail field is not being set when a new User is created, which violates the IX_Users_NormalizedEmail unique database constraint.

How to Fix the Backend
You will need to apply the following fixes to your backend repository (NewsandVulSBE_ST) and deploy it to your remote server:

1. Fix the Sign-Up Error (AuthController.cs): In src/NotesAndFileBackend.Api/Controllers/AuthController.cs, update the SignUp method to populate the NormalizedEmail:

csharp
var user = new User
{
    Email = request.Email,
    NormalizedEmail = request.Email.ToUpperInvariant(), // Add this line
    DisplayName = request.DisplayName,
    PasswordHash = HashPassword(request.Password),
    Status = "ACTIVE"
};
2. Troubleshoot the /files Endpoint: The /files endpoint throws a 500 during JSON serialization or entity resolution. Since .NET 8 is used, the Include(f => f.PublicShares) may be trying to auto-include unmapped relations, or the remote database might be missing the IsActive or RevokedAt columns (meaning the latest Entity Framework Migrations haven't been applied to newsandvulst.mrpent.us).

Make sure to run dotnet ef database update against the production database to ensure all tables and columns are up to date.
If you would like to test against your local backend to reproduce and fix this, you can temporarily change BaseUrl in Services/ThreatIntelApiService.cs from https://newsandvulst.mrpent.us/api/v1/ to your local Docker environment http://localhost:5001/api/v1/ and try loading the Files page.

Let me know if you would like me to change the URL to localhost to help you test, or if you can apply those backend fixes!